services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    working_dir: /app
    volumes:
      - .:/app
    command: ["python", "project_starter.py"]
    init: true
